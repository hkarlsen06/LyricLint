import { PHONE_LAYOUT_QUERY } from '$lib/interaction/phone-layout.js';

export { PHONE_LAYOUT_QUERY, STACKED_BREAKPOINT } from '$lib/interaction/phone-layout.js';

/** Task views and compact media apply to touch devices in the stacked layout. */
export const PHONE_WORKSPACE_QUERY = PHONE_LAYOUT_QUERY;

/** Touch task views and their explicit commands share the stacked-layout boundary. */
export const STACKED_BREAKPOINT = '68rem';
export const PHONE_LAYOUT_QUERY = `(pointer: coarse) and (max-width: ${STACKED_BREAKPOINT})`;

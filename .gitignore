node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/.svelte-kit-capture
/build-capture

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Dev certificates (per-machine, made by `bun run certs`)
certs

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results

# Wrangler local secrets for services/*
.dev.vars

# CI-only Worker configuration; the published domain and kill switch are retained.
/services/rules-assistant/wrangler.release.json
